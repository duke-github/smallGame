server:
  port: 9000
## MyBatis
mybatis:
  # 搜索指定包别名
  typeAliasesPackage: com.ccw.**.mapper
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 加载全局的配置文件
  configLocation: classpath:mybatis/mybatis-config.xml


spring:
  datasource:
      url: jdbc:mysql://127.0.0.1:3306/alumni?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
      username: root_test
      password: ccwle6@163
      driverClassName: com.mysql.cj.jdbc.Driver
  data:
    redis:
      database: 1
      host: 127.0.0.1
      port: 6379
      password:


